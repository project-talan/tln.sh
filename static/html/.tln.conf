module.exports = {
  tags: async (tln) => [],
  options: async (tln) => [],
  dotenvs: async (tln) => ['.env'],
  inherits: async (tln) => ['react', 'sonarqube', 'nginx', 'docker'],
  depends: async (tln) => ['node-14.2.0'],
  env: async (tln, env) => {
    env.TLN_UID = [env.TLN_UID, env.TLN_COMPONENT_ID].join('.');
  },
  steps: async (tln) => [],
  components: async (tln) => []
}